<%= form_for @artist, :html => {class: "form-inline"} do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= field_set_tag "" do %>
    <div class="control-group">
      <%= f.label :name %><br />
      <%= f.text_field :name, value: @artist_name, class: "input-xlarge" %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "#{@action} Artist", class: "btn btn-primary" %>
  </div>
<% end %>
